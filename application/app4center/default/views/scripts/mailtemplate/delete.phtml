<?php echo $this->render("header.phtml") ?>
<script type="text/javascript">
$().ready(function() {
	$("#post_form").validate({
	   event: "keyup",
	   rules: {},
	   messages: {},
	   submitHandler: function() {
			SeedMsgBox('正在保存中，请稍候...');
			var options = {
			    target:     '#SeedMsgTxt',
			    url:        '<?php echo $this->BaseUrl();?>/mailtemplate/delete',
			    success: function() {
			    }
			};
			$('#post_form').ajaxSubmit(options);
		}
	});
});
</script>

<h1>删除邮件模板</h1>
<div class="textnav">
  <p>
    <input class="admin_bnt2" name="" type="button" value="返回" onclick="history.back();"/>
  </p>
</div>
<form name="post_form" method="post" action="" id="post_form" >
  <table class="admin_list">
    <tr>
      <td>编号</td>
      <td>模板名称</td>
    </tr>
    <?php if(is_array($this->temps)):?>
    <?php foreach($this->temps as $temp):?>
    <tr>
      <td><input type="checkbox" id="temp_id<?php echo ($temp['temp_id'])?>" name="temp_id[]" checked value="<?php echo ($temp['temp_id'])?>">
        <?php echo ($temp['temp_id'])?></td>
      <td><?php echo ($temp['temp_name'])?></td>
    </tr>
    <?php endforeach; ?>
    <?php endif;?>
  </table>
  <div class="batch">
    <input class="admin_bnt2" type="submit" name="" value="确认删除" onclick="return confirm('确认删除选择记录？')">
  </div>
</form>
<?php echo $this->render("footer.phtml") ?> 