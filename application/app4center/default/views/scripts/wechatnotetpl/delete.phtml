<?php echo $this->render("header.phtml") ?>
<script type="text/javascript">
$().ready(function() {
	$("#post_form").validate({
	   event: "keyup",
	   rules: {},
	   messages: {},
	   submitHandler: function() {
			SeedMsgBox('正在保存中，请稍候...');
			var options = {
			    target:     '#SeedMsgTxt',
			    url:        '<?php echo $this->BaseUrl();?>/wechatnotetpl/delete',
			    success: function() {
			    }
			};
			$('#post_form').ajaxSubmit(options);
		}
	});
});
</script>
<h1>删除微信通知模板</h1>
<div class="textnav">
  <p>
    <input class="admin_bnt2" name="" type="button" value="返回" onclick="history.back();"/>
  </p>
</div>
<form name="post_form" method="post" action="" id="post_form" >
  <table class="admin_list">
    <tr>
      <th>编号</th>
      <th>模板名称</th>
      <th>模板标题</th>
    </tr>
    <?php if(is_array($this->tpls)):?>
    <?php foreach($this->tpls as $temp):?>
    <tr>
      <td><input type="checkbox" id="temp_id<?php echo ($temp['nt_id'])?>" name="nt_id[]" checked value="<?php echo ($temp['nt_id'])?>">
        <?php echo ($temp['nt_id'])?></td>
      <td><?php echo ($temp['nt_name'])?></td>
      <td><?php echo ($temp['nt_desc'])?></td>
    </tr>
    <?php endforeach; ?>
    <?php endif;?>
  </table>
  <div class="batch">
    <input type="submit" name="" value="确认删除" class="admin_bnt2">
  </div>
</form>
<?php echo $this->render("footer.phtml") ?>
